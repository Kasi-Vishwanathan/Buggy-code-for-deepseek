#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a = 5, b = 3;
    double result = (a + b) * (a - b); // Incorrect formula
    cout << "Result: " << result << endl;
    return 0;
}
